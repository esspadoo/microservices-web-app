server {
    listen 80;

    location / {
        root /usr/share/nginx/html;
        try_files $uri $uri/ =404;
    }
    
    location /api/v1/searcher/ {
    	proxy_pass http://searcher:8881/api/v1/searcher/;
    }

}
